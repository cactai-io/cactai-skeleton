import type { MorphState } from '@cactai-io/types';
import type { MUIConfig } from '../types/mui.types.js';
import { MUIStore } from '../store/MUIStore.js';
import { InputRouter } from '../input/InputRouter.js';
export interface MUIRenderOutput {
    morph_state: MorphState;
    component?: unknown;
    code?: string;
    error?: string;
    surface_type?: string;
}
export type RenderCallback = (output: MUIRenderOutput) => void;
export declare class MUIShell {
    private store;
    private agent;
    private streamController;
    private inputRouter;
    private skillRegistry;
    private skillComposer;
    private generativeFallback;
    private complexityAssessor;
    private autoRegistrar;
    private surfaceRegistry;
    private config;
    private renderCallback;
    private stagedInspectorContext;
    private constructor();
    static init(config: MUIConfig): Promise<MUIShell>;
    onRender(callback: RenderCallback): void;
    loadSkillRegistry(registry: Record<string, unknown>): void;
    registerSurface(surfaceType: string, component: unknown): void;
    submitInput(content: string): Promise<void>;
    stageInspectorContext(ctx: import('@cactai-io/types').InspectorContext): void;
    clearInspectorContext(): void;
    getStagedInspectorContext(): import('@cactai-io/types').InspectorContext | null;
    activateSkill(skillId: string): void;
    deactivateSkill(skillId: string): void;
    registerDeveloperWrittenSkill(opts: {
        skill_id: string;
        name: string;
        platform: string;
        artifact_types: string[];
        code: string;
        component?: unknown;
    }): void;
    private handleHandoff;
    private runRenderCycle;
    private actOnDecision;
    private pendingGeneration;
    confirmRender(): void;
    reportDefect(_description?: string): void;
    dismissError(): void;
    retryLastInput(): Promise<void>;
    private emitRender;
    private buildArtifactIndex;
    getStore(): MUIStore;
    getInputRouter(): InputRouter;
    private static detectDevice;
    private static computeBreakpoint;
}
