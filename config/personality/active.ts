// config/personality/active.ts
// GENERATED FILE — DO NOT EDIT DIRECTLY.
// Set during workflow Stage 3 by the agent.
// Change by updating skeleton.config.json personality.active_id
// and asking the agent to regenerate.
//
// The active personality drives the tone and behavior of AI responses
// in the developer's user-facing app. It is separate from the DevShell's
// personality (which is set in the DevShell settings panel).

export const activePersonalityId: string | null = null;

// Personality metadata — populated by the agent
export const activePersonality = {
  id:          null as string | null,
  name:        '',
  tone:        '',
  description: '',
};
